import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://SpeedFox03.github.io',
  base: 'https://github.com/SpeedFox03/emberfox-site', // le nom exact de ton repo
});